name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: nimble test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        nim-version: ['2.2.6']
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Nim ${{ matrix.nim-version }}
        uses: jiro4989/setup-nim-action@v2
        with:
          nim-version: ${{ matrix.nim-version }}
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install dependencies
        run: nimble install -y -d

      - name: Install optional test backends
        # unittest2 is the backend for matrix cells 3, 4, 7 (selected via
        # -d:tripwireUnittest2). It is intentionally NOT in `requires`
        # because consumers using std/unittest do not need it.
        run: nimble install -y unittest2

      - name: Run tests
        run: nimble test
