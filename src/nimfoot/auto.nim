## nimfoot/auto.nim — umbrella TRM module (Chunk 1 stub).
##
## Consumer projects opt in via `--import:"nimfoot/auto"` in their
## config.nims so every module gets the TRM pattern-matching
## templates installed. Full facade + plugin TRMs are emitted in
## Chunk 7 (Task G1). This stub exists so nimble test targets
## referencing `--import:nimfoot/auto` resolve during foundation
## chunks when G1 has not yet landed.
